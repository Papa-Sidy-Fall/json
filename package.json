{
  "name": "whatsapp-clone-api",
  "version": "1.0.0",
  "description": "API backend pour WhatsApp Clone",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}